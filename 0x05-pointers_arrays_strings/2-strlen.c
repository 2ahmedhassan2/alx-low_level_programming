#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - main
 * @s: parameter
 * Return: Always 0
 *
 */

int _strlen(char *s)
{
char *s = "How";
len = _strlen(s);
 _putchar("%d", len);
}
