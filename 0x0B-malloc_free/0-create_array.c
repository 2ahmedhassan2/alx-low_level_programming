#include <stdio>
#include <stdlib>
#include "main.h"

/**
 * create_array - checks uppercase
 * @size: parameter
 * @c: parameter
 * Return: Null
 */

char *create_array(unsigned int size, char c)
{
if (size == 0)
return (NULL);
}
