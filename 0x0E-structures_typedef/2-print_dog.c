#include <stdlib.h>
#include "dog.h"

/**
 * print_dog - checks uppercase
 * @d: parameter
 * Return: Null
 */

void print_dog(struct dog *d)
{
if (d == NULL)
{
return ("nil");
}
d.name = "Poppy";
d.age = 3.5;
d.owner = "Bob";
print_dog(&d);
if (name == NULL)
{
return ("Name = (nil)");
}
return (0);
}
