#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * @str: Arg
 * @dest: Arg
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src)
{
*dest[100] = "This is ", *src[] = "programiz.com";.
strcat(str1, str2);
puts(str1);
puts(str2);
return 0;
}
